# 复刻剧情活动

**用户纠正（2026-09-19）：** 本文实测包含 Agent 分析与手工整理方案，不能据此宣称正式自动化已经闭环。头像与作业的自动获取链路仍待实现；真实方案仅保存在忽略的 `cache/game/strategies/`。后续 Agent 应实现可独立运行的获取/更新/解析能力，不重复依赖 Agent 手工准备，详见 `AGENTS.md` 和待办表。

## 活动与布局（2026-09-19）

- **实测／数据库来源**：当前 `hatsune_schedule` 中活动 10171，`original_event_id=10150`，开放 2026/09/16 12:00:00～09/23 23:59:59。`event_story_data.value=10171` 对应“幻惑的妖精 ‐盛夏乐园中舞动的身影‐”。查询复刻时独立筛选 `original_event_id > 0`，不能只取同期活动的第一条。
- **用户要求**：每期完整执行一次，包含困难关卡和高难首领，最后抽完当前拥有的奖励券；完成后不重复做日常。复刻身份与布局分别判断，未来复刻可能使用新版列表布局。
- **实测**：冒险页“复刻”(321,422) 与“剧情活动”(411,422) 是两个入口。本期是地图布局。页面特征：右上普通/困难、右下讨伐证交换。新版列表识别使用“活动关卡·首领”或首页“关卡·首领”，不按复刻身份推断布局。
- 证据：`cache/agent/story_event/revival_adventure.png`、`revival_home.png`；仅 960×540 实机验证。

## 首通（实测）

- 普通 1-1～1-10，困难 1-1～1-5。关卡详情有自动推进勾选框 (715,334)，挑战后编队点“下一步”，进入“自动推进设定”。勾选不停止、立即发动后按战斗开始。游戏自行处理每关结算的“下一步”，脚本无需抢点。
- 普通自动推进预览／实际消耗 47 体力，困难 44；两组均全部首通，每个困难关卡剩余 2/3。证据 `cache/agent/revival/auto_plan.png`、`auto_finished.png`，早期普通结果 `cache/agent/revival/auto_stopped.png`。
- 结束先出现“自动推进结束／由于是最终关卡”，确认后仍要处理最终关结算“下一步”及“自动推进报酬一览／关闭”。不能看到普通胜利页就当整段完成。
- 地图会横向自动移动；“下一关”黄色标记可定位待首通节点，点击标记下方约 80 像素。模板 `images/revival_next.png` 是匿名 UI 局部。
- 完成后 N 最终节点在右侧地图 x≈520，编号全屏 OCR 常漏，局部放大 `(0,150,600,450)` 可读。H 五个节点同屏，H1-5 在(850,310)，描边编号即使放大仍可能漏；点击该已知节点后必须用详情标题“1-5”和顶部金星双重确认。两组最终详情均已核验3星。
- 普通首领“混沌发电机”等级25，消耗20券；2026-09-19 已一次击败，解锁困难。
- 困难首领等级50，消耗30券；**用户明确授权本期一次使用当前通关队伍试打**，已成功击败。授权仅限 10171，不推广至其他活动或高难/SP。
- 详情左上难度小标签全屏 OCR 会把“困难”读成“联国”；需局部放大识别。右上讨伐数数字框 `(891,28,926,56)` 可读取孤立0，过大的裁框可能返回未知。
- 首领结算插入活动角色对话：粉色姓名板 `(200,394,355,413)`＋白色宽对话框，下方 (775,495) 推进。判断对话应优先于背景地图，地图需要稳定确认。不是剧情菜单，不应点击想象中的菜单。

## 高难作业（来源资料，实战状态另见待办）

- [混沌发电机复刻 VH](https://gamewith.jp/pricone-re/article/show/458470)：魔法队厄里斯、兰法、美空、姬塔（术士）、薇奥莉特，全 SET；兰法、美空、姬塔需要专武1，另外两名未开启。
- [复刻 SP](https://gamewith.jp/pricone-re/article/show/458408)：火物理队涅妃＝涅菈、碧（工作服）、凤凰、望（解放者）、艾拉；碧/艾拉 SET，碧与望专武1开启。具体要求保存 `cache/game/strategies/revival_teams.yml`。
- **识别实测**：薇奥莉特三色信息圆点受头像背景影响，灰度模板最低分略低于旧门槛；新增三色固定位置联合判定，仍连续两帧确认。旧剑徽／星数用例均通过；匿名局部 `test/fixtures/story_event/badge_filled_orbs.png`。

## 高难与 SP 实战（2026-09-19）

- VH 五人实时核验达标后一次击败，伤害 5,000,000，首领券 78→48。SP 火物理队五人核验达标，一次挑战跨过三个阶段并通关，首领券 48→18，首页出现“表演赛”，讨伐证增至 1274。表演赛是已经通过 SP 后的重复练习入口，任务不重复挑战。证据：`cache/agent/revival/vh_audit.json`（最早一次曾因未知徽章失败，后已修复）、`roster.json`、`sp_audit.json`、`sp_after.png`。
- SP 解锁后自动进入旧版活动首页：活动关卡(559,202)、水都的回忆(559,274)、讨伐证交换(574,347)、活动剧情(818,348)、活动任务(910,441)。首领面板在右上，有独立滚动区域。新引导会指向“特别”行，再出现两页可可萝说明；其对话框与地图小对话同形。
- 本期 SP 在高难击败后解锁；高难当日打完，地图入口灰锁不能再打开详情读讨伐数。复查时用本期首页已出现“表演赛”确认 SP 及前置高难已通过，不重复点击锁定高难。普通/困难仍从详情核对合计讨伐数。
- 旧地图首页的“活动剧情”分两行，不能只用全屏 OCR 的连续“活动剧情”判断。新版列表首页和旧版首页须分别识别。
- **SET 修正**：国服角色开启立即发动是蓝色圆标，不是金色 SET 字样。正常战斗时头像右上蓝圆可被 UB 动画遮挡；应打开战斗菜单暂停后核验。菜单五人中心 x=306/394/481/569/656、y=237，蓝圆在各自右上；点击小头像能切换个人 SET。菜单“SET关闭”表示不是全员开启，不能用它判断所有人都未 SET。
- 菜单 AUTO 位于(480,358)，必须核实“AUTO开启”，点击特效消失前 OCR 可能漏字，要等待。全员/逐人状态核验完成后点“返回”(335,437)。本次 SP 最终 SET 为 `[关,开,关,关,开]`，AUTO 开；证据 `cache/agent/revival/battle_after_settings.png`。
- 初次校准发现全局立即发动默认为关闭，直接点头像无法核对 SET；当前使用暂停菜单逐人设置，已实战通过。战斗快速结束期间的设置检查不能误报撤退失败，应进入胜利结算；SP 阶段剧情也可能暂时取代战斗画面。

## 剧情与回忆

- 活动剧情是中央弹窗“活动剧情一览”，关闭(480,480)，列表可滚动。当前主剧情无未读提示；完整一次任务会检查列表，已读不重复播放。
- 列表提示“初次阅读剧情时，可以获得奖励道具”会命中通用领奖弹窗关键字，必须优先识别列表，避免自动关闭后反复打开。此问题已用实机列表发现并补离线回放。
- 水都的回忆是独立竖向列表，右侧新内容标记约 x=507，点击同一行 x=750、标记 y+40；读完返回列表并获得30宝石。必须滚动到底核对未读消失，不能只看首屏四条。证据 `cache/agent/story_event/revival_memories_map.png`、`cache/agent/revival/memory_*.png`。
- 2026-09-19 已读完15篇，每篇30宝石；最后出现“收取报酬”弹窗并获得称号“好，辛苦了Summer”。弹窗标题在顶部中央，关闭(480,480)，须先处理再继续列表判断。

## 活动任务与兑换（2026-09-19 实测）

- 活动任务左上标题，顶部每日/普通/特别/称号四栏，各栏“全部收取”(840,437)，灰色表示当前无可领奖励；收取报酬弹窗关闭后继续检查。四栏领取完，讨伐证由1274增至1684。
- 讨伐证交换是抽奖奖池，最多100张一批，按钮次数受本轮剩余奖励和券余额限制（本次观察20、54等尾数）。消费直接发生，没有单独确认；开点前核对“次数交换”和“使用N张”一致且不大于余额，回首页后核对实际扣除。
- 稀有报酬展示页“查看已获得道具”(480,427)，结果页“取消”(370,425)返回余额；余额用完后结果页改为“确认”(480,425)。不使用“再次交换”绕过余额核对。
- 奖池抽空会出现 COMPLETE／“已获得第N轮报酬列表中的全部报酬”，确认后弹出“当前的列表”。只有“重置目标报酬”和“普通报酬”两项“剩余0/总数”均确认才点“重置”(480,480)。随后有“重置完毕／重置了报酬列表”确认(480,373)，注意结果页可能被该弹窗替代，不应在背景结果页文字上阻塞等待取消。这里是抽空后切换奖池，非首领进度重置。
- 本次1684张全部兑换。零余额的孤立0全屏OCR可能遗漏，先查完整数字栏；未知时只在两枚兑换按钮均灰色的情况下局部读取 `(909,431,929,454)`，严格确认0。证据 `cache/agent/revival/exchange_empty.png`、`cache/agent/story_event/revival_exchange_*.png`。
- 抽空奖池会新增活动任务奖励，兑换后再次检查四类任务并兑换新取得的券，循环直到无新任务且余额零；最多5轮，不能仅在兑换前领取一次任务。

## 正式入口验收（2026-09-19）

- `scripts/daily/revival_event.py` 最终返回 `complete`、`pending: []`。N1-10/H1-5均3星，普通/困难首领合计讨伐数已确认，SP表演赛标记已确认，主剧情与15篇水都回忆无未读，抽奖后新增三项普通任务也已领取，四栏再次检查无可领，讨伐证余额0。
- 最终完整检查 `battles: []`，没有重复开战。报告与截图在 `cache/daily/revival_event/`；回执按配置账号与本期开放时间保存在 `cache/daily/revival_state/`。
- 紧接着以普通沙箱权限重跑正式入口，远程情报刷新不可用时读取已有有效数据库，0.7秒返回 `already_complete`（10171），未枚举窗口/启动OCR/操作游戏。
- 离线：`test_event.py` 47项、`test_revival.py` 14项通过；新增回归覆盖并行活动的复刻查询、账号期次隔离、完成跳过、列表式复刻保留待办、一次试打限制、SET、三色信息帧、兑换扣券及奖池保护、独立首领滚动、剧情列表提示误判。
- 本期首通是在开发中分段校准并实战完成；正式入口验收基于已通关状态。下一期全新首通连续衔接及未来列表式复刻仍在待验证表，不把本次完成检查等同于这些场景已验证。
- 提交前审查补充：SP 不读取普通/VH 详情的讨伐次数坐标，避免把不同布局中的数字误认为通关；兑换遇到数据连接遮罩时等待服务端更新，不用背景旧余额判定扣券。两项离线回放通过，当前活动回归47项、复刻回归16项；未为此重复消耗游戏资源。
