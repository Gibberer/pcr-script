# 新版剧情活动

复刻身份不等于地图布局，未来复刻也可能采用此列表布局。活动情报与 UI 布局独立判断，独立一次性任务见[复刻记录](revival-events.md)。2026-09-19 共用战斗 SET 已修正为暂停菜单逐人核验蓝色圆标，实战证据和坐标也在该记录中；列表式首通本身仍待首日验证。

## 本期实测范围

2026-09-12，国服 “I Wish 后传 And I will 牢牢紧握在手中”。账号原本已通过所有关卡和首领。观测到 N-1～N-10、H-1～H-3，三类首领：剧本模式、特别、特别战斗＋。因此首通实战未验证。

本次最终验证：13 关通过检查、三类首领跳过均正常；H-3 剩余一次扫荡已完成并核实次数变为 0；主剧情、回忆录均无剩余可读项目；四栏活动任务检查完成；新增 15 张活动券全部兑换，余额确认 0。最终报告为 `partial`，原因是剩余 16 体力不足以继续 H-2/H-1，不是流程错误。证据：`cache/daily/story_event/report.json`、`cache/daily/event_final.log`。

离线测试共 32 项通过，覆盖实际截图、真实 OCR 数字、专武/星数轮播、撤退路径、连接中旧列表、结算恢复及禁止 ADB 回退。未通关首领实战仍列于待验证清单。

## 页面与顺序

- **2026-09-21 完整日常实测补验**：每日登录奖励弹窗自动关闭；困难 H-1/H-2/H-3 一次多选、倍率3，预览各3次/180体力，结算后三关均0/3，体力750→570。未首通、未挑战首领。证据固定保存于 `cache/daily/runs/20260921-162528-33264-b67a00/task-evidence/story_event/` 的 `event_login_reward.*`、`hard_bulk_plan.*`、`hard_bulk_confirm.*`、`hard_bulk_finished.*`，日志同运行目录。混合剩余次数尚未触发，继续待验证。下文较早的“批量消费待实测”描述保留为历史状态，以本条为最新验证范围。

- 2026-09-13 真实失败截图：每天首次进入活动可能弹出“获得活动登录奖励”（标题 ROI 240,110,720,185），居中“关闭”(480,372)。这是每天的登录奖励，与活动首日过图无关；需要先关闭弹窗，再判断活动首页或点击底部导航。旧入口漏处理导致循环等待并超时。证据 `cache/agent/event_login_20260913/error.png/json`；对应 OCR 已加入回放测试。
- 冒险菜单中的“主线关卡”卡片与主线地图标题同名。左上角 (45,0,240,65) 出现“主线关卡”代表地图，先点返回 (32,30) 回到冒险菜单；不能因为地图没有活动按钮就判定活动未开放。2026-09-13 恢复运行时发现并修复此误判。
- 活动首页：关卡·首领 (116,395)、兑换 (270,415)、活动剧情 (810,430)、任务 (786,35)。
- 关卡页顶部标题“活动关卡·首领”；关卡页签 (578,76)、首领页签 (820,76)，系统扫荡 (859,128)。
- 当前每日顺序：直接困难批量扫荡 → 可选普通扫荡 → 首页有标记才进入剧情/本期回忆录 → 有任务标记才检查四栏 → 兑换活动券。首通/首领默认关闭，不每日遍历 13 关确认状态。
- 首领列表的“通关”要按所在行匹配，不能借用相邻首领标记。已通关首领不每天重打。特别战斗＋详情有模式与剩余次数；详情右上“放弃”会重置整场进度，禁止当作战斗撤退。

## 扫荡（页面已实测，三关批量消费待实测）

- 系统“一键扫荡”先进入“关卡一览”。2026-09-13 用户确认可多选三关；当前实现清除记忆选项后一次勾选 H-1/H-2/H-3，并设置共享倍率 3。默认三行已可见，仅列表记住其他位置时恢复滚动；三关均 0/3 就直接取消，不清选择、不滚动。H 每次 20 体力，每日每关 3 次，N 每次 10。
- 第一次“一键扫荡”打开“一键扫荡确认”；核对所选关卡、各关跳过次数和总消耗，再点“挑战”。三关各三次应为 180 体力。此按钮直接消耗体力，结算后核对全部所选 H 均 0/3。今日已由旧流程耗完次数，新批量消费仅回放验证；剩余次数不同能否按各关截断也待实机确认，预览不符时停止。
- 次数框的孤立数字“1”也可能被全屏 OCR 漏掉，单纯放大仍可能被文字检测器过滤。数值框先做局部检测识别；未检测到文字时再直接识别整框（`use_det=False, use_cls=False`）。数字 0 需要保留检测裁边才能达到足够置信度；不能用缺字或低置信结果猜测数值。
- 结果可能很快返回“关卡一览”，未必能捕获独立结果页。必须核对剩余次数减少（或普通关卡体力减少）后再判断成功；不确定时不能再次点击消耗。
- 确认按钮关闭后先出现灰色“正在进行数据连接”的旧列表，此时旧次数尚未更新，需等待连接结束再核对。当前实际还会出现“扫荡结果”弹窗，确认后才返回有新次数的列表；可从结果弹窗恢复核验，不重复发起消费。
- 体力不足记录未完成并继续后续领奖；不购买体力、不重置次数。
- 2026-09-12 重复日复测：空选择时点击“解除所有勾选”会出现“未勾选任何关卡”提示，遮住 H-2 的剩余次数。必须等提示消失再读数，不能直接报未知或猜次数。
- 扫荡确认标题的“一”偶尔被 OCR 漏掉，读成“键扫荡确认”；兼容这一已实测变体，同时继续强制核对本批关卡及总体力。中断留在此确认页时，入口先取消原计划并重读进度，不能直接确认旧消费。

## 任务、兑换、剧情（实测）

- 活动任务有“每日、普通、特别、称号”四栏。“全部收取”亮蓝可用，暗蓝禁用。扫荡及读剧情后先看首页任务标记，有提示才进入四栏收取。
- 兑换页原生“一键交换”直接消耗显示的全部券数，并自动处理奖池重置；用消耗数与余额一致性检查，交换后核对余额归零。
- 全屏 OCR 会漏识别孤立的数字 0，可对已知余额框放大 OCR；缺失不能视为零。
- 体力降到个位数会被全屏检测漏掉（2026-09-12 实测数字 4）。用户明确要求：**体力数值不是硬性条件，不能因读不出体力阻断主流程**。困难批量不读可用体力数值，只按剩余次数计划，以游戏“体力不足”提示/按钮状态停止扫荡并继续领奖，不再逐关试扫。可选普通扫荡仍用一次全屏体力读数优化次数，未知按一次；普通结算可用实际结果弹窗，无数值且未见结果时停止追加扫荡并继续领奖。
- 主剧情有语音选择、菜单跳过、梗概确认、动画隐藏跳过热点、奖励确认等分支。动画下载期间有整体超时，不应把加载画面当作新的按钮。
- 本期“复制世界回忆录”是独立额外剧情：艾拉世界横向节点、诗夏世界纵向节点，通过 NEW 逐个打开。根页终章没有 NEW，而是右上角宝石“×30”，外观类似锁；本期点击后实际可读。其他活动的小游戏不能假设共用此流程。

### 首页提示标记（2026-09-13）

用户说明：剧情与追忆全部看完后，首页未领取符号会消失，不需要进页面核查。对照 09-12 有奖励与 09-13 清空后的真实图，使用粉色菱形组件识别：剧情 ROI `(692,355,738,402)`，回忆录 `(78,253,120,301)`，任务 `(787,3,832,45)`。颜色 HSV H=145～179、S≥70、V≥140，组件宽高 10～22、面积 60～250、填充比 0.35～0.75；小装饰不算提示。截图先归一化到 960×540。对应六个匿名局部截图保存在 `test/fixtures/story_event/*_marked.png` / `*_clear.png`。其他活动更换入口布局后需重新核对位置。

兑换首页余额框 `(260,340,315,377)` 明确读到 0 时直接跳过；全屏 OCR 可能漏掉该零，此时仍进入兑换页核实，不能将未知当零。

### 2026-09-13 修复与简化验证

卡住原因是入口漏处理每日登录奖励弹窗；另修复主线地图标题误判为冒险菜单导致活动 unavailable。失败证据 `cache/agent/event_login_20260913/`。关闭登录弹窗本身只有真实 OCR 回放验证；恢复实机时旧弹窗已消失，下次每日重置补验。

补跑旧逐关流程完成 H-3/H-2/H-1 各三次、四栏活动任务及 155 张券兑换，首领战斗为空，日志 `cache/daily/event_login_fix_20260913.log`。随后新流程实机验证：三关 0/3 直接退出列表，剧情、回忆录、任务无标记均未进入，兑换余额核实 0，报告 complete、pending 为空，日志 `cache/daily/event_fast_20260913.log`。47 项离线检查通过，包括多选计划、消费预览、混合剩余次数、重复确认保护、首页标记正反例。三关批量实际消费仍等待下次有次数时验证。

来源：[本期 SP 作业](https://gamewith.jp/pricone-re/article/show/516586)、[SP＋作业](https://gamewith.jp/pricone-re/article/show/516593)。后续不同首领需新作业，不能只替换活动名。

## 2026-09-12 重复日复测

默认首通/首领关闭配置已实测通过入口检查，H-2 扫荡一次并核实次数减少；低体力时停止后续扫荡，继续检查四类活动任务，15 张新获得的券全部兑换，首领战斗记录为空。完整日志 `cache/daily/event_repeat_enabled.log`；按用户要求取消强制体力 OCR 后的低体力扫荡复测 `cache/daily/event_optional_stamina.log` 正常返回 `partial`，没有异常。数字缺失时按一次并依据游戏不足提示停止的分支有离线回放检查。
